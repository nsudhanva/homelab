apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: llama-models
  namespace: llama
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 15Gi
  storageClassName: local-path
