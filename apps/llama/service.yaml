apiVersion: v1
kind: Service
metadata:
  name: llama-server
  namespace: llama
  labels:
    app: llama-server
spec:
  type: ClusterIP
  selector:
    app: llama-server
  ports:
    - name: http
      port: 8080
      targetPort: 8080
