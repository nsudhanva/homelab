apiVersion: v1
kind: Service
metadata:
  name: alert-summarizer
  namespace: monitoring
  labels:
    app.kubernetes.io/name: alert-summarizer
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: http
  selector:
    app.kubernetes.io/name: alert-summarizer
