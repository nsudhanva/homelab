apiVersion: v1
kind: Service
metadata:
  name: actual-budget
  namespace: actual-budget
  labels:
    app: actual-budget
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 5006
      targetPort: http
      protocol: TCP
  selector:
    app: actual-budget
