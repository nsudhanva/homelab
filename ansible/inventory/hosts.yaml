all:
  children:
    k8s_control_plane:
      hosts:
        control-plane-1:
          ansible_host: 192.168.1.100
          ansible_user: ubuntu
          node_type: nvidia

    k8s_workers:
      hosts:
        worker-1:
          ansible_host: 192.168.1.101
          ansible_user: ubuntu

    k8s_nodes:
      children:
        k8s_control_plane:
        k8s_workers:

    gpu_nvidia:
      hosts:
        control-plane-1:

    gpu_intel:
      hosts: {}

    cpu_only:
      hosts:
        worker-1:
